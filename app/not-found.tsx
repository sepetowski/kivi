import { NotFoundPage } from '@/components/notFound/NotFoundPage';

const NotFound = () => {
	return <NotFoundPage notFoundMessage='Could not find requested resource' />;
};

export default NotFound;
